#include <stdio.h>

int main(){
    int i, n;
    puts("Qual número que quer os multiplos?");
    scanf("%d", &n);
    for(i=1; i <= 1000; i++){
        if(i % n == 0) printf("%d ", i);
    }
    printf("\n");
    return 0;
}
